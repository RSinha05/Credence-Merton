# ── Core Quantitative ──
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# ── Visualization ──
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Data Acquisition ──
yfinance>=0.2.18
requests>=2.28.0

# ── API Backend ──
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# ── Database ──
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.0       # PostgreSQL driver
aiosqlite>=0.19.0             # SQLite async for dev/testing

# ── Async Task Queue ──
celery[redis]>=5.3.0
redis>=5.0.0

# ── ML & Early Warning (Phase 2) ──
scikit-learn>=1.3.0
xgboost>=2.0.0
arch>=6.2.0                  # For GARCH(1,1) equity volatility forecasting
tslearn>=0.6.3               # For DTW + K-Means time-series trajectory clustering
pyyaml
